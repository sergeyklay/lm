feat(chrome): size the closing block to the terminal it lands on

The closing block measured its own width and ignored the terminal it
was printed into, so a narrow window wrapped it mid-sentence. It now
reads the terminal's columns and sizes itself to what it lands on.
