# Written by a verb into the working tree and removed again on the happy path.
# They survive an interrupted run, and neither belongs in a commit.
CHANGELOG.tmp
*.lmbak

# 136 packages the Node runner needs; package-lock.json pins them.
node_modules/
